{
    "scripts": {
        "start-backend": "source venv/bin/activate && uvicorn backend.main:app --reload --port 8000 --app-dir .",
        "start-frontend": "npm --prefix frontend run start-app",
        "dev": "concurrently \"npm run start-backend\" \"npm run start-frontend\""
    },
    "dependencies": {
        "concurrently": "^8.2.2"
    }
}